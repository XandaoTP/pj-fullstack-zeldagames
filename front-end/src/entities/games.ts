import { Loading } from '../components/loading';
export type Games = {
    id: number;
    title: string;
    image: string;
    description: string;
    content: string;
    createdAt: string;
    Loading: boolean
};
