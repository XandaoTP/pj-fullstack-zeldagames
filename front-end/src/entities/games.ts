import { Loading } from '../components/loading';
export type Games = {
    id: number;
    title: string;
    picture: string;
    subtitle: string;
    content: string;
    createdAt: string;
    Loading: boolean
};
